services:
  db:
    image: postgres:16
    container_name: job_tracker_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: jobtracker
      POSTGRES_PASSWORD: jobtracker_dev_pw
      POSTGRES_DB: job_tracker
    ports:
      - "5432:5432"
    volumes:
      - job_tracker_pgdata:/var/lib/postgresql/data

volumes:
  job_tracker_pgdata: